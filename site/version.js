/* Version de l'application — source unique.
   Incrémenter à CHAQUE mise à jour du site : elle alimente l'affichage du
   footer et la version du cache du service worker (invalidation offline). */
const APP_VERSION = '1.4.0';
